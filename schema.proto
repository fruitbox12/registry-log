message Entry {

    // public key
    required bytes key = 1;

    // signature
    required bytes sig = 2;

    // update time
    required uint64 created = 3;
    required uint64 updated = 4;

    // domain name
    required string name = 5;

    // domain target (URI)
    required string target = 6;
}
